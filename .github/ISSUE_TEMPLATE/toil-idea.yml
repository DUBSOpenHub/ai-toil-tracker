name: Toil Automation Idea
description: Log a repetitive task that could be automated
title: "[TOIL] "
labels: [toil, triage]
body:
  - type: markdown
    attributes:
      value: |
        📍 **Where to review all submissions:**
        Use the links in the repo README to view all ideas, triage queue, in-progress, and completed items.

  - type: input
    id: submitter
    attributes:
      label: Your name and GitHub handle
      description: So the team knows who submitted this and can tag you on updates
      placeholder: e.g. Jane Smith (@janesmith)
    validations:
      required: true

  - type: input
    id: team
    attributes:
      label: What team or group are you on?
      description: Used to group and filter toil ideas by team in the dashboard
      placeholder: e.g. Platform, DevEx, SRE, Marketing
    validations:
      required: true

  - type: textarea
    id: toil
    attributes:
      label: What's the toil?
      description: Describe the repetitive task
      placeholder: e.g. "I manually copy deploy logs into a spreadsheet after every release"
    validations:
      required: true

  - type: dropdown
    id: frequency
    attributes:
      label: How often does it happen?
      options:
        - 🔴 Multiple times per day
        - 🟠 Daily
        - 🟡 Multiple times per week
        - 🔵 Weekly
        - ⚪ Monthly or less
    validations:
      required: true

  - type: dropdown
    id: time-per-occurrence
    attributes:
      label: How long does it take each time?
      options:
        - "< 5 minutes"
        - "5–15 minutes"
        - "15–30 minutes"
        - "30–60 minutes"
        - "> 1 hour"
    validations:
      required: true

  - type: dropdown
    id: people-affected
    attributes:
      label: Who is affected?
      options:
        - Just me
        - 2–3 people
        - 4–6 people
        - Entire team
        - Multiple teams / org-wide
    validations:
      required: true

  - type: dropdown
    id: category
    attributes:
      label: What area does this toil fall under?
      options:
        - 🔧 CI/CD & Builds
        - 💬 Communication & Reporting
        - 👥 Onboarding & Access
        - 🔍 Code Review & Quality
        - 🖥️ Operations & Infrastructure
        - 📝 Documentation
        - 📊 Data & Analytics
        - 🔄 Other
    validations:
      required: true

  - type: checkboxes
    id: bonus-factors
    attributes:
      label: Bonus factors (check all that apply)
      description: These add weight to the priority score
      options:
        - label: "❌ Error-prone — this toil frequently leads to mistakes"
        - label: "😤 Morale killer — this toil is particularly frustrating"
        - label: "🔗 Blocking — this toil blocks other people or processes"

  - type: textarea
    id: automation-idea
    attributes:
      label: Automation idea (if any)
      description: How might an agent or script handle this?
      placeholder: Optional - leave blank if you're not sure
    validations:
      required: false
